% result = a \ b with fixed cardinalities.
var set of 1..4: a;
var set of 1..4: b;
var set of 1..4: d;
constraint set_card(a, 3);
constraint set_card(b, 2);
constraint set_diff(a, b, d);
constraint set_card(d, 2);
solve satisfy;
