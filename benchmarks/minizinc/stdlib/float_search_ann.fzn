% Exercise float_search annotation (precision ignored).
% Domain is a singleton so search terminates immediately.
var 0.5..0.5: x;
solve :: float_search([x], 0.001, input_order, indomain_split, complete) satisfy;
