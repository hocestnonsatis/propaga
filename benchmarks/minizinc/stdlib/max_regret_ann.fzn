% Exercise largest / max_regret variable selectors.
var 1..3: a;
var 5..9: b;
constraint int_le(a, 2);
constraint int_le(b, 8);
solve :: int_search([a, b], max_regret, indomain_min, complete) satisfy;
