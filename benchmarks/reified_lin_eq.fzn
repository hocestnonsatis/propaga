% Reified linear eq: b == (2*x + y == 5), x fixed to 2, b fixed to 1
var 1..3: x;
var 1..3: y;
var 0..1: b;
constraint int_lin_eq_reif([2, 1], [x, y], 5, b);
constraint int_eq(x, 2);
constraint int_eq(b, 1);
solve satisfy;
