% Constant float-array element (1-based).
array [1..2] of float: xs = [1.5, 2.5];
var 1..2: i;
var 0.0..5.0: v;
var 2.5..2.5: target;
constraint array_float_element(xs, i, v);
constraint int_eq(i, 2);
constraint float_eq(v, target);
solve satisfy;
