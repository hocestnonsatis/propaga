% Reified linear le: b == (x + y + z <= 6), b fixed to 1
var 1..3: x;
var 1..3: y;
var 1..3: z;
var 0..1: b;
constraint int_lin_le_reif([1, 1, 1], [x, y, z], 6, b);
constraint int_eq(b, 1);
solve satisfy;
