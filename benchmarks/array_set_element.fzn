% Constant set-array element (1-based).
array [1..2] of set of int: xs = [{1}, {1, 2}];
var 1..2: i;
var set of 1..3: s;
constraint array_set_element(xs, i, s);
constraint int_eq(i, 2);
constraint set_card(s, 2);
solve satisfy;
