% Bound-tightening onto a float hole must wipe the domain (UNSAT).
var 0.0..5.0: x;
var 2.0..2.0: hole;
var 3.0..3.0: y;
constraint float_ne(x, hole);
constraint float_lin_eq([1.0, 1.0], [x, y], 5.0);
solve satisfy;
