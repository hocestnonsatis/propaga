% Permutation via global_cardinality (each value 1..3 appears exactly once)
array [1..3] of int: cards = [1, 2, 3];
array [1..3] of var 1..3: x;

constraint global_cardinality(cards, x);
constraint int_lin_eq([1, 1, 1], [x[1], x[2], x[3]], 6);

solve satisfy;
