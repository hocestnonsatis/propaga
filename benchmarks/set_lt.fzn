% Proper set subset (set_lt) with cardinality tightening.
var set of 1..3: a;
var set of 1..3: b;
var 1..1: one;
constraint set_in(one, a);
constraint set_lt(a, b);
constraint set_card(b, 2);
solve satisfy;
