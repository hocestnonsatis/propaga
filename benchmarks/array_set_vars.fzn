% array of set vars + 1-based array_var_set_element.
array [1..2] of var set of 1..3: xs;
var 1..2: i;
var set of 1..3: s;
constraint set_card(xs[1], 2);
constraint set_card(xs[2], 1);
constraint array_var_set_element(xs, i, s);
constraint int_eq(i, 1);
constraint set_card(s, 2);
solve satisfy;
