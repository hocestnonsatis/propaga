% Reified linear eq reif=0: b == (x + y != 4), x fixed to 2
var 1..3: x;
var 1..3: y;
var 0..1: b;
constraint int_lin_eq_reif([1, 1], [x, y], 4, b);
constraint int_eq(x, 2);
constraint int_eq(b, 0);
solve satisfy;
