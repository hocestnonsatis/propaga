% Native set inequality.
var set of 1..3: a;
var set of 1..3: b;
var 1..1: one;
var 2..2: two;
constraint set_in(one, a);
constraint set_in(two, a);
constraint set_card(a, 2);
constraint set_ne(a, b);
constraint set_card(b, 1);
solve satisfy;
