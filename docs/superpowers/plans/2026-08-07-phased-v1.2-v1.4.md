# Propaga Aşamalı Geliştirme Planı (v1.2 → v1.4+)

> **For agentic workers:** Implement task-by-task on `main`. Checkbox syntax tracks progress.

**Goal:** v1.1.0 sonrası kalite (v1.2), MiniZinc ekosistem uyumu (v1.3), rekabetçi arama (v1.4+).

**Architecture:** Mevcut 9-crate stack korunur. Exact reals / GPU / `.mzn` parser kapsam dışı. MiniZinc solver-specific FlatZinc için full redefinitions değil, yaygın alias → stdlib map.

**Tech Stack:** Rust 1.88+, FlatZinc 1.6, MiniZinc 2.9+ (v1.3 CI)

---

## Faz A — v1.2 Polish

- [x] A4 ROADMAP / COMPATIBILITY / CHANGELOG Unreleased iskeleti
- [x] A1 `ceil`/`floor`/`round` hole-list + unit-width gap checks ([`float.rs`](../../../crates/propaga-domains/src/float.rs))
- [x] A3 Optimize/lex/Pareto portfolio (`PortfolioSearch::optimize*`, CLI `--workers`, Model API)
- [x] A2 Typed set/float learning via `ForbiddenAssignmentPropagator` ([`dfs.rs`](../../../crates/propaga-search/src/dfs.rs))
- [ ] A5 Release v1.2.0 (version bump, tag, memories shipped) — kullanıcı isteğiyle

### Kabul (v1.2)

- `cargo test --workspace` + clippy yeşil
- `--workers` BnB/lex/Pareto yolunda çalışır
- Float wipeout + `learning` → `nogoods_learned > 0`

---

## Faz B — v1.3 Ecosystem

- [x] B1 `fzn_*` / `array_int_lt` alias shim (`aliases.rs` + parse/compile remaps)
- [x] B2 MiniZinc install + `flatzinc-full-compat-report` CI job (`minizinc-fresh`)
- [x] B3 `incomplete` search semantics (`SearchConfig::incomplete`)
- [ ] B4 Release v1.3.0 — kullanıcı isteğiyle
- [ ] Triage / extend aliases from `minizinc-fresh` failures as they appear

---

## Faz C — v1.4+ Competitive

- [x] C1 Warm-start + LNS (int optimize)
- [x] C2a Variable capacity for `cumulative` / `fzn_cumulative`
- [ ] C2b Variable-size `diffn` (+ stronger edge-finding)
- [ ] C3 Timed FlatZinc perf corpus / nightly Criterion
- [ ] C4 Release v1.4.0

---

## Notlar

- Typed learning şu an sabit assignment bloğu; first-UIP backjump yalnızca int path’te.
- Sparse hole’lar continuum ceil/floor/round preimage’lerini boşaltamaz; A1 unit-width gap + hole-list best-effort.
